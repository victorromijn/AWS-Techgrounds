# First we have to activate the module random
# To produce multiple integers
# then we limit the loop result with a range function by 5
# to produce the random numbers between 0 and 100, we run the ".randint" from the random module.
# print(random.randrange(0,100))


import random

for variabele_i in range(5):
	print(random.randint(0, 100))
	#print(variabele_i)